{
  "$defs": {
    "Payload": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "new_state": {
          "$ref": "#/$defs/ProjectState"
        },
        "previous_state": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectState"
            },
            {
              "type": "null"
            }
          ]
        },
        "project_id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/ProjectType"
        }
      },
      "required": [
        "new_state",
        "project_id",
        "title",
        "type"
      ],
      "title": "ProjectLifecyclePayloadSchema",
      "type": "object"
    },
    "ProjectState": {
      "description": "Runtime project state. Not serialized to project.yaml; tracked by registry.",
      "oneOf": [
        {
          "description": "Installed but not running.",
          "enum": [
            "installed"
          ],
          "type": "string"
        },
        {
          "description": "Stopped after running.",
          "enum": [
            "stopped"
          ],
          "type": "string"
        },
        {
          "description": "Currently transitioning to running.",
          "enum": [
            "starting"
          ],
          "type": "string"
        },
        {
          "description": "Currently running and serving.",
          "enum": [
            "running"
          ],
          "type": "string"
        },
        {
          "description": "Currently transitioning to stopped.",
          "enum": [
            "stopping"
          ],
          "type": "string"
        },
        {
          "description": "Failed to start or crashed.",
          "enum": [
            "failed"
          ],
          "type": "string"
        },
        {
          "description": "Soft-deleted, awaiting cleanup.",
          "enum": [
            "archived"
          ],
          "type": "string"
        }
      ]
    },
    "ProjectType": {
      "oneOf": [
        {
          "description": "Native Plurora project: has project.yaml at repo root, manifest references Plurora packages.",
          "enum": [
            "plurora_native"
          ],
          "type": "string"
        },
        {
          "description": "External project wrapped by an adapter package.",
          "enum": [
            "external_wrapped"
          ],
          "type": "string"
        },
        {
          "description": "External project running in an agent workspace (no wrapping).",
          "enum": [
            "external_workspace"
          ],
          "type": "string"
        }
      ]
    }
  },
  "$id": "urn:plurora:schema:event:host/project.installed:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Payload schema for event kind host/project.installed.",
  "properties": {
    "kind": {
      "const": "host/project.installed"
    },
    "payload": {
      "$ref": "#/$defs/Payload"
    }
  },
  "title": "host/project.installed",
  "type": "object"
}
