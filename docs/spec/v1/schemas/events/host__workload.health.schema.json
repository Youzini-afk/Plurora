{
  "$defs": {
    "Payload": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "properties": {
        "failure_count": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "port_lease_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "previous_ready": {
          "type": "boolean"
        },
        "probe": {
          "$ref": "#/$defs/WorkloadHealthProbe"
        },
        "ready": {
          "type": "boolean"
        },
        "reason": {
          "type": "string"
        },
        "route_id": {
          "type": "string"
        }
      },
      "required": [
        "failure_count",
        "previous_ready",
        "probe",
        "ready",
        "reason",
        "route_id"
      ],
      "title": "WorkloadHealthEventPayload",
      "type": "object"
    },
    "WorkloadHealthProbe": {
      "properties": {
        "kind": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    }
  },
  "$id": "urn:plurora:schema:event:host/workload.health:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Payload schema for event kind host/workload.health.",
  "properties": {
    "kind": {
      "const": "host/workload.health"
    },
    "payload": {
      "$ref": "#/$defs/Payload"
    }
  },
  "title": "host/workload.health",
  "type": "object"
}
